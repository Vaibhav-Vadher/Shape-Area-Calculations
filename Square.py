# Python program to compute Area of square
print("Area of Square")

# input / assignment
length = int(input("Enter the Side : "))

# compute / process
area = length ** 2

# output / results
print(f"Length = {length} m")
print(f"\nArea of square = {area} sq.m")
