import math
# Python program to compute Surface Area of sphere
print("Surface Area of sphere")

# input / assignment
radius = int(input("Enter the Radius : "))

# compute / process
area = 4 * math.pi * radius**2

# output / results

print(f"Radius  = {radius} m")
print(f"\nSurface Area of sphere = {area} sq.m")